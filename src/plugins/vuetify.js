import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

import vuetify_colors_config from '../config/vuetify_colors'

Vue.use(Vuetify);

export default new Vuetify(vuetify_colors_config);
